[project]
name = "{{project_name}}"
version = "0.1.0"
description = ""
authors = [
    {name = "{{author_name}}", email = "{{author_email}}"},
]
dependencies = [
   "launchkit"
]
requires-python = ">=3.11"
readme = "README.md"
license = {text = "MIT"}

[tool.pdm.dev-dependencies]
dev = [
    "pytest>=7.4.3",
]

[tool.pdm.scripts]
start.cmd = "launchkit {{project_name}}:actions"
start.env_file = ".env"

dev.cmd = "launchkit {{project_name}}:actions"
dev.env_file = ".env"

test.cmd = "pytest"
test.env_file = ".env"